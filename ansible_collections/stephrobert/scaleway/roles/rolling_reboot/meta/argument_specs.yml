---
argument_specs:
  main:
    short_description: Reboot a named inventory group in batches
    description:
      - >-
        Reboots one batch at a time and waits for each batch to come back before
        touching the next. A machine that is not running is left alone and
        named: a rolling reboot that starts machines has done something nobody
        asked for.
      - >-
        The waiting is observation, never a duration. The action module watches
        each machine leave C(running) and return, and refuses to conclude when it
        never saw it change.
    options:
      scaleway_rolling_reboot_selector:
        type: dict
        default: {}
        description:
          - >-
            What to roll through, said the same way everywhere: exactly one of
            C(group), C(name) or C(tags), plus C(match) set to C(all) or C(any)
            when several tags are given.
          - >-
            C(all) is the default, because it is the narrower selection: for an
            operation that writes, erring on the safe side means touching fewer
            machines, never more.
          - >-
            An ambiguous name is refused rather than settled, and an unknown key
            is refused rather than selecting nothing, because a typo and an
            empty fleet look alike.
          - >-
            C(kind) says what is being rolled through, C(instance) by default or
            C(k8s_node) for the nodes of a Kubernetes pool. It is not a
            criterion: the selector still carries exactly one of those, and the
            same grammar holds for a machine and for a node.
          - >-
            For C(k8s_node), C(group) means the pool, which groups nodes the way
            an inventory group groups machines, and the list comes from the API
            rather than from the inventory, which serves no Kubernetes. C(tags)
            is not accepted there: a node carries none, and passing a pool's off
            as its own would add a hop you cannot see.
      scaleway_rolling_reboot_zones:
        type: list
        elements: str
        description:
          - >-
            The zones to ask when the selector designates pool nodes; the
            regions are derived from them. Ignored for Instances, which come
            from the inventory with their zone.
      scaleway_rolling_reboot_group:
        type: str
        default: ""
        description:
          - >-
            The inventory group to roll through. Empty means do nothing and say
            so, because a roll with no target would reboot whatever the
            inventory happens to hold.
      scaleway_rolling_reboot_batch_size:
        type: int
        default: 1
        description:
          - >-
            How many machines to reboot at once. One at a time is the safe
            default; a larger batch is a decision.
      scaleway_rolling_reboot_wait_timeout:
        type: int
        default: 300
        description:
          - >-
            Bound, in seconds, for the observation the action module performs on
            each machine. Not a sleep.
